{
  "name": "contact-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local terminal runner for LinkedIn contact verification.",
  "scripts": {
    "verify": "node src/verify-contacts.mjs",
    "check": "node --check src/verify-contacts.mjs"
  },
  "dependencies": {
    "playwright": "^1.44.0"
  }
}
